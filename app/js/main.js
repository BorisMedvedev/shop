
$(function () {

  $('.top-slider__list').slick({
    dots: true,
    arrows: false,
    fade: true,
    autoplay: true,
    autoplaySpeed: 3000

  });

  $(".rateyo").rateYo({
    starWidth: "40px"
  }

  );


});